<Window x:Class="WpfApp2.Views.testWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2.Views"
        mc:Ignorable="d"
        Title="testWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- 定义样式 -->
        <Style x:Key="ModernExpanderStyle" TargetType="Expander">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Margin="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <!-- 标题部分 -->
                                <ToggleButton x:Name="HeaderSite" 
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                              FontSize="14"
                                              FontWeight="Medium"
                                              Foreground="{TemplateBinding Foreground}"
                                              Padding="{TemplateBinding Padding}"
                                              IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              >
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="HeaderBorder"
                                                                Background="#f0f0f0"
                                                                BorderBrush="#cccccc"
                                                                BorderThickness="1"
                                                                CornerRadius="4"
                                                                Padding="8">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>

                                                                <ContentPresenter x:Name="ContentSite"
                                                                                  Content="{TemplateBinding Content}"
                                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                  Margin="{TemplateBinding Padding}"
                                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                                  RecognizesAccessKey="True"/>

                                                                <!-- 展开/折叠图标 -->
                                                                <Path x:Name="ExpandPath"
                                                                      Grid.Column="1"
                                                                      Data="M0,0 L0,2 L4,6 L8,2 L8,0 Z"
                                                                      Fill="#666666"
                                                                      HorizontalAlignment="Center"
                                                                      Margin="8,0,0,0"
                                                                      VerticalAlignment="Center"
                                                                      Width="8"
                                                                      Height="6">
                                                                    <Path.RenderTransform>
                                                                        <RotateTransform x:Name="ExpandPathRotateTransform" Angle="0" CenterX="4" CenterY="3" />
                                                                    </Path.RenderTransform>
                                                                </Path>
                                                            </Grid>
                                                        </Border>

                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="HeaderBorder" Property="Background" Value="#e8e8e8" />
                                                            </Trigger>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="HeaderBorder" Property="CornerRadius" Value="4,4,0,0" />
                                                               
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Foreground" Value="#999999" />
                                                                <Setter TargetName="HeaderBorder" Property="Background" Value="#f5f5f5" />
                                                                <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="#dddddd" />
                                                                <Setter TargetName="ExpandPath" Property="Fill" Value="#bbbbbb" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>

                                <!-- 内容部分 -->
                                <ContentPresenter x:Name="ExpandSite"
                                                  Grid.Row="1"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Visibility="Collapsed"
                                                  Margin="0"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                    <ContentPresenter.Style>
                                        <Style TargetType="ContentPresenter">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=Expander}}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentPresenter.Style>
                                </ContentPresenter>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 焦点视觉样式 -->
        <Style x:Key="ExpanderHeaderFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="0" Stroke="#60000000" StrokeThickness="1" StrokeDashArray="1 2" RadiusX="1" RadiusY="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- 左侧区域 -->
        <Border Background="#f9f9f9" BorderBrush="#dddddd" BorderThickness="0,0,1,0">
            <TextBlock Text="左侧内容区域" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
        </Border>

        <!-- 右侧区域 - 包含多个Expander -->
        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0">
            <!-- 第一个Expander -->
            <Expander  Header="个人信息" Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200" Padding="10">
                    <StackPanel>
                        <TextBlock Text="姓名: 张三" Margin="0,5" />
                        <TextBlock Text="年龄: 30" Margin="0,5" />
                        <TextBlock Text="职业: 软件工程师" Margin="0,5" />
                        <TextBlock Text="邮箱: zhangsan@example.com" Margin="0,5" />
                        <TextBlock Text="电话: 123-456-7890" Margin="0,5" />
                        <TextBlock Text="地址: 北京市海淀区中关村" Margin="0,5" />
                        <TextBlock Text="公司: 科技有限公司" Margin="0,5" />
                        <TextBlock Text="部门: 研发部" Margin="0,5" />
                        <TextBlock Text="职位: 高级工程师" Margin="0,5" />
                    </StackPanel>
                </ScrollViewer>
            </Expander>

            <!-- 第二个Expander -->
            <Expander  Header="技能列表" Background="White" Margin="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200" Padding="10">
                    <StackPanel>
                        <TextBlock Text="编程语言:" Margin="0,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="- C#" Margin="5,0,0,0" />
                        <TextBlock Text="- Java" Margin="5,0,0,0" />
                        <TextBlock Text="- Python" Margin="5,0,0,0" />
                        <TextBlock Text="- JavaScript" Margin="5,0,0,0" />

                        <TextBlock Text="框架:" Margin="10,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="- .NET Framework" Margin="5,0,0,0" />
                        <TextBlock Text="- ASP.NET MVC" Margin="5,0,0,0" />
                        <TextBlock Text="- Entity Framework" Margin="5,0,0,0" />
                        <TextBlock Text="- React" Margin="5,0,0,0" />

                        <TextBlock Text="数据库:" Margin="10,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="- SQL Server" Margin="5,0,0,0" />
                        <TextBlock Text="- MySQL" Margin="5,0,0,0" />
                        <TextBlock Text="- MongoDB" Margin="5,0,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </Expander>

            <!-- 第三个Expander -->
            <Expander  Header="项目经验" Background="White" Margin="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200" Padding="10">
                    <StackPanel>
                        <TextBlock Text="项目名称: 企业资源规划系统" Margin="0,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="描述: 开发一套完整的企业资源规划系统，包括财务、人力资源、采购等模块。" Margin="5,0,10,0" />
                        <TextBlock Text="技术栈: C#, ASP.NET MVC, SQL Server" Margin="0,0,0,10" />

                        <TextBlock Text="项目名称: 电子商务平台" Margin="10,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="描述: 设计并实现一个电子商务平台，支持在线购物、支付、订单管理等功能。" Margin="5,0,10,0" />
                        <TextBlock Text="技术栈: Java, Spring Boot, MySQL, React" Margin="0,0,0,10" />

                        <TextBlock Text="项目名称: 数据分析工具" Margin="10,5,0,0" FontWeight="Bold" />
                        <TextBlock Text="描述: 开发一个数据分析工具，用于处理和可视化大量数据。" Margin="5,0,10,0" />
                        <TextBlock Text="技术栈: Python, pandas, matplotlib, Django" Margin="0,0,0,10" />
                    </StackPanel>
                </ScrollViewer>
            </Expander>
        </StackPanel>
    </Grid>
</Window>
